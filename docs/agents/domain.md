# Domain Docs

This is a single-context repo.

Engineering skills should read root-level `CONTEXT.md` when it exists, then check `docs/adr/` for architectural decisions relevant to the area being changed.

If these files do not exist yet, proceed silently. The producer skill (`/grill-with-docs`) creates them lazily when project language or decisions become explicit.
